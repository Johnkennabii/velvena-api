{
  "/customers/{id}": {
    "delete": {
      "tags": ["Customers"],
      "summary": "Hard delete a customer",
      "description": "Permanently deletes a customer from the database.",
      "operationId": "hardDeleteCustomer",
      "security": [{ "bearerAuth": [] }],
      "parameters": [
        { "name": "id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
      ],
      "responses": {
        "200": {
          "description": "✅ Customer hard deleted successfully",
          "content": {
            "application/json": {
              "schema": { "$ref": "#/components/schemas/HardDeleteCustomerResponse" }
            }
          }
        }
      }
    }
  }
}